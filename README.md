# A Test Version of mobile devices kernel generation
我们建立了一个移动端kernel生成的benchmark和一套标准的评测pipeline。benchmark包含了一定的数据集，pipeline可以轻松稳定的评测一个算子在移动端的性能。
## quick start
首先clone MNN的仓库到本地，将本仓库放在MNN的根目录下。
然后用extra_MNN中的文件夹替换MNN中对应的部分。
测试。

